// Accessing Elements Withing Array

// Access the first element

const StudentAges = [20, 30, 25, 23, 21, 27];
const firstAge = StudentAges[2];
const checkAge = StudentAges[StudentAges.length - 1];
console.log(firstAge);
console.log(checkAge);
