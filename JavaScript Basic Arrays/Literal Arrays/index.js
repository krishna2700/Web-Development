// ? Array Literals

// Syntax

const demoSyntax = ["Krishna", false, 23, [], {}];

const studentAges = [20, 21, 22, 23, 24, 25];
console.log(studentAges);

// Declaring and Array of Strings
const dayOfWeeks = [
  "Monday",
  "Tuesday",
  "Wednesday",
  "Thursday",
  "Friday",
  "Saturday",
  "Sunday",
];
console.log(dayOfWeeks);

// Declaring Array of Mix Types

const mixTypes = [23, "Krishna", true];
console.log(mixTypes);

// Empty data

const EmptyData = [];
console.log(EmptyData);
