// Length Method

// Array Size (.length)

const colors = ["Red", "yellow", "green", "Black"];
const sizeOfColors = colors.length;
console.log(sizeOfColors);

// Modify the length of Array

colors.length = 2;
console.log(colors);
