// ? Array Constructor

// creatin an Array using Array Constructor

// Create an Array with empty data
const emptyData = new Array();
console.log(emptyData);

// Create an Array with a Prededined Sizes (5)

const sizeDefinedArray = new Array(5);
console.log(sizeDefinedArray);

//  Creating an Array witr elemnets
const fruitArr = new Array("Apple,Banana,Kiwi");
console.log(fruitArr);

// Creating use Mix data types

const ixedDataTypes = new Array("Krishna", 23, true);
console.log(ixedDataTypes);
