// string lastindexof()

const text = "orange apple, apple banana";
const lastIndex = text.lastIndexOf("apple");
console.log(lastIndex);

// Specifying the from Index

const result = text.lastIndexOf("apple", 12);
console.log(result);
